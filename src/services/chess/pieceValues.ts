/** Standard relative material values in pawns. Shared by ChessAdapter (material score) and ChessContextEngine (material balance, capture significance). */
export const PIECE_VALUE: Record<string, number> = { p: 1, n: 3, b: 3, r: 5, q: 9, k: 0 };
